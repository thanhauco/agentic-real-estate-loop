# The system runs fully offline in deterministic MOCK mode by default.
# (All numeric analysis is real; only natural-language polishing is mocked.)
#
# To use a real LLM for narrative polishing, set ONE provider below.

# --- OpenAI ---
# OPENAI_API_KEY=sk-...
# REL_LLM_MODEL=gpt-4o-mini

# --- Azure OpenAI ---
# AZURE_OPENAI_API_KEY=...
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
# AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini
# AZURE_OPENAI_API_VERSION=2024-06-01

# --- NVIDIA Build (build.nvidia.com, OpenAI-compatible) — cloud model glm5.2 ---
# Get a key at https://build.nvidia.com (starts with "nvapi-"). NVIDIA's endpoint is
# OpenAI-compatible, so set OPENAI_API_KEY + OPENAI_BASE_URL + REL_LLM_MODEL=z-ai/glm-5.2.
NVIDIA_API_KEY=
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1
NVIDIA_MODEL=z-ai/glm-5.2
